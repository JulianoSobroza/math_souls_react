
  # Gamified Math Platform

  This is a code bundle for Gamified Math Platform. The original project is available at https://www.figma.com/design/III5QFtdiJ2FruWdh20Ged/Gamified-Math-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # math_souls_react
